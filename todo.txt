TODO:

->	TILDE expands when as separate argument, or being the first item
	and followed by '/'

->	$ expansions expand everywhere and only do so if found (cd lol$HOMElol)

->	setenv pepe=$SHELL (or any other expansion in one liner)

->	echo

->	quotes: translate quotes which comes first

-> ./main.c should give correct error

execve error found: setenv pepe=$MAIL ; setenv pepe $MAIL ; clear


moi"lol\nmoi"

